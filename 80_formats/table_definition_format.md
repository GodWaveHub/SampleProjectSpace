# テーブル定義書フォーマット

## テーブル名: {テーブル名}

### 概要
- **テーブル物理名**: {テーブル物理名}
- **テーブル論理名**: {テーブル論理名}
- **用途**: {テーブルの用途・説明}
- **作成日**: {作成日}
- **最終更新日**: {最終更新日}
- **作成者**: {作成者}

### カラム定義

| No | カラム物理名 | カラム論理名 | データ型 | 長さ | 精度 | NOT NULL | PRIMARY KEY | DEFAULT | 備考 |
|----|-------------|-------------|----------|------|------|----------|-------------|---------|------|
| 1  | {column_name_1} | {カラム論理名1} | {データ型} | {長さ} | {精度} | {YES/NO} | {YES/NO} | {デフォルト値} | {備考} |
| 2  | {column_name_2} | {カラム論理名2} | {データ型} | {長さ} | {精度} | {YES/NO} | {YES/NO} | {デフォルト値} | {備考} |
| 3  | {column_name_3} | {カラム論理名3} | {データ型} | {長さ} | {精度} | {YES/NO} | {YES/NO} | {デフォルト値} | {備考} |

**注意**: カラム論理名は必須項目です。業務上の意味を理解しやすい日本語名称を設定してください。

### 制約定義

#### 主キー制約
- **制約名**: {制約名}
- **対象カラム**: {対象カラム}

#### 外部キー制約
| 制約名 | 対象カラム | 対象カラム論理名 | 参照テーブル | 参照カラム | 参照カラム論理名 | 更新ルール | 削除ルール |
|--------|------------|----------------|-------------|------------|----------------|------------|------------|
| {制約名1} | {対象カラム1} | {対象カラム論理名1} | {参照テーブル1} | {参照カラム1} | {参照カラム論理名1} | {CASCADE/RESTRICT/SET NULL} | {CASCADE/RESTRICT/SET NULL} |

#### ユニーク制約
| 制約名 | 対象カラム | 対象カラム論理名 | 備考 |
|--------|------------|----------------|------|
| {制約名1} | {対象カラム1} | {対象カラム論理名1} | {備考} |

#### チェック制約
| 制約名 | 対象カラム | 対象カラム論理名 | 制約条件 | 備考 |
|--------|------------|----------------|----------|------|
| {制約名1} | {対象カラム1} | {対象カラム論理名1} | {制約条件} | {備考} |

### インデックス定義
詳細は別途 `{テーブル名}_Index_Definition.md` を参照

### データ例
```sql
-- サンプルデータ挿入例
INSERT INTO {テーブル物理名} ({カラム1}, {カラム2}, {カラム3}) 
VALUES 
  ({値1}, {値2}, {値3}),
  ({値1}, {値2}, {値3});
```

### DDL文
```sql
-- テーブル作成DDL
CREATE TABLE {テーブル物理名} (
    {カラム1} {データ型} {制約},
    {カラム2} {データ型} {制約},
    {カラム3} {データ型} {制約},
    PRIMARY KEY ({主キーカラム}),
    FOREIGN KEY ({外部キーカラム}) REFERENCES {参照テーブル}({参照カラム})
);
```

### 関連テーブル
- {関連テーブル1}: {関係性の説明}
- {関連テーブル2}: {関係性の説明}

### 特記事項
- {その他の重要な情報}
- {注意事項}
- {運用上の制約}
- {1,000レコード時のテーブルデータの物理サイズ[MB]}